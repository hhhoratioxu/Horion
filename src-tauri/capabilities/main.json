{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum permissions for the Horion main window during phase 1.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
