{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum permissions for the Horion main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
